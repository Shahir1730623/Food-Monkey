<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodapp" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodApp" >
        <activity
            android:name=".Customer.Restaurant.RestaurantSinglePage"
            android:exported="true" >
        </activity>
        <activity
            android:name=".Customer.Restaurant.RestaurantHomePage"
            android:exported="true" >
        </activity>
        <activity
            android:name=".Customer.CustomerDashboard"
            android:exported="true" >
        </activity>
        <activity
            android:name=".Customer.Shop.CustomerShop"
            android:exported="true" />
        <activity
            android:name=".IntroductoryScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>